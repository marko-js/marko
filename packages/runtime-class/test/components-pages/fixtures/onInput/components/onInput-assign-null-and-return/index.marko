class {
    onInput(input) {
        this.input = null;

        return {
            name: input.name.toUpperCase()
        }
    }
}

<div>
    Hello <span.name>${input ? input.name : '(no input)'}</span>!
</div>
