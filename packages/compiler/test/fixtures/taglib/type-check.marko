<probe-badge/badge label="hello" count=1 size="small" items=["hello"] model={ key: "value" } id="badge" onClick() { badge().setAttribute("active", ""); }>
  <span>child</span>
</probe-badge>
import type { Input as BadgeInput } from "<probe-badge>";
export interface Input { badge?: BadgeInput; }
