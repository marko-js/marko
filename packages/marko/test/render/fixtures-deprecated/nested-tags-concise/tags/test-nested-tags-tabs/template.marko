<var tabs=input.tabs/>

<div class="tabs">
    <ul>
        <li for(tab in tabs)>
            ${tab.title}
        </li>
    </ul>
    <div class="tab-content">
        <div class="tab" for(tab in tabs)>
            <invoke tab.renderBody(out) if(tab.renderBody)/>
        </div>
    </div>
</div>