class {
    onInput(input) {
        this.state = {
            name: input.name
        };
    }
}

<div.foo>
    <span>Hello ${state.name}!</span>
    <div.body>
        <${input.renderBody}/>
    </div>
</div>