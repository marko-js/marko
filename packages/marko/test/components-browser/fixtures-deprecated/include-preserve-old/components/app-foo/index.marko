class {
    onInput(input) {
        this.state = {
            name: input.name,
            body: input.renderBody
        };
    }
}

<div.foo>
    <span>Hello ${state.name}!</span>
    <div.body>
        <include(state.body)/>
    </div>
</div>