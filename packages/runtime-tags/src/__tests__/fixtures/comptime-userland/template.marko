<let/claps=0/>

<-hero variant="launch" title="Meet Comptime">
  <button onClick() { claps++; }>clap ${claps}</button>
</-hero>

<hero variant="live" title=`runtime ${input.title}`>
  <p>still a component</p>
</hero>
