import { resolveAfter } from "../../utils/resolve";

<let/show=false/>
<button onClick() { show = true }>show</button>
<define/Content>
  <await|v|=resolveAfter("defined")>${v}</await>
</define>
<try>
  <@placeholder>define loading</@placeholder>
  <if=show><div content=Content/></if>
</try>
<wrapper show=show>
  <await|v|=resolveAfter("wrapped")>${v}</await>
</wrapper>
<try>
  <layout>
    <await|v|=resolveAfter("laid out")>${v}</await>
  </layout>
  <@placeholder>layout loading</@placeholder>
</try>
<try>
  <@placeholder>layout attr loading</@placeholder>
  <if=show><layout content=Content/></if>
</try>
<try>
  <await|v|=resolveAfter("pressed")>
    <base-button>${v}</base-button>
  </await>
  <@placeholder>button loading</@placeholder>
</try>
