<define/Level|{ depth }|>
  <countdown-buttons depth=depth - 1/>
</define>

<button onClick(e) { e.target.textContent = "clicked" }>${input.depth}</button>
<if=input.depth>
  <Level depth=input.depth/>
</if>
