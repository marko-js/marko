// size: 127 (min) 90 (brotli)
(_2._script(`a0`, ($scope) =>
  _2._on($scope.a, `click`, function () {
    document.getElementById(`el`).innerHTML =
      $scope.M === void 0 ? `index missing` : $scope.M;
  }),
),
  init());
