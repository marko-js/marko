<div>
  <if=input.show>
    <section title=input.kind>
      <if=input.detail>
        <p>${input.text}</p>
      </if>
      <else>
        <em>${input.summary}</em>
      </else>
    </section>
  </if>
</div>
