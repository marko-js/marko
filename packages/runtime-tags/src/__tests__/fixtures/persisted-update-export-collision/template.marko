import { createPatch, have } from "./data";

<let/count=0/>
<button onClick() { count++ }>clicked ${count}</button>
<if=input.id>
  <const/label=createPatch(input.id)>
  <p>${label} / ${have(input.id)}</p>
</if>
<else>
  <p>nothing selected</p>
</else>
