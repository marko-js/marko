<main>
  <if=input.show>
    <card title=input.title>
      <if=input.alt>
        <em>A:${input.note}</em>
      </if>
      <else>
        <strong>B:${input.note}</strong>
      </else>
    </card>
  </if>
</main>
