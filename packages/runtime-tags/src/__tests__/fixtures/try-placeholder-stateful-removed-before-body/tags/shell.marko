export interface Input {
  status: string;
}
<lifecycle
  onMount() { console.log("shell mounted", input.status) }
  onDestroy() { console.log("shell destroyed", input.status) }
/>
<div data-status=input.status>
  <child workspace={ id: "a", name: "A" } active=true/>
</div>
