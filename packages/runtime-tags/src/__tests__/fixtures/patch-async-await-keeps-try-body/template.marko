<main>
  <h1>${input.title}</h1>
  <try>
    <if=input.show>
      <await|value|=input.promise>
        <em>${value}</em>
      </await>
    </if>
    <else>
      <em>closed</em>
    </else>
    <@placeholder><em>loading</em></@placeholder>
  </try>
</main>
