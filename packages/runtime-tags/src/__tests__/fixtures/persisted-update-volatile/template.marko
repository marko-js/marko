import { nextStamp } from "./stamp";
<let/count=0/>
<h1>${input.title}</h1>
<div id="stamp">stamp:${nextStamp()}</div>
<const/label=`label:${nextStamp()}`/>
<span id="label">${label}</span>
<button onClick() { count++ }>${count}</button>
