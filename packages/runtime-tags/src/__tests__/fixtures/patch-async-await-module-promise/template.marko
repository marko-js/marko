import { fetchThing } from "./data.js";

<let/n=0/>
<button class="n" onClick() { n++ }>${n}</button>
<main>
  <if=input.show>
    <await|value|=fetchThing()>
      <em>${value}</em>
    </await>
  </if>
</main>
