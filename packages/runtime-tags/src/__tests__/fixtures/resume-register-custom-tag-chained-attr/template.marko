<let/count=0>
<button#inc onClick() { count++ }>${count}</button>

<wrapper kind="h1">chained string: not registered</wrapper>
<wrapper kind=count % 2 ? "h2" : "h1">chained state string: not registered</wrapper>
