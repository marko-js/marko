import { resolveAfter } from "../../utils/resolve";
<let/clickCount = 0/>
<button onClick() {
  clickCount++
}>inc</button>
<try>
  <await|value|=resolveAfter(clickCount)>
    ${value}
  </await>
  <@placeholder>
    LOADING...
  </@placeholder>
</try>
