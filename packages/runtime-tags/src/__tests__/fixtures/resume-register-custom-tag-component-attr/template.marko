import Card from "./tags/card.marko";

// A static value that is not a string may be a component that serializes
// the body for its own state, so this caller registers the child's body.
<heading type=Card/>
<heading type="h1"/>
