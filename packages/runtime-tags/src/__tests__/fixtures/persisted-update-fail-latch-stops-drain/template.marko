import Lineup from "./tags/lineup.marko"
import Ticker from "./tags/ticker.marko"

<let/count=0/>
<button.count onClick() { count++ }>clicked ${count}</button>
<h1>${input.title}</h1>
<stage view=Lineup performers=input.performers/>
<${input.view === "ticker" ? Ticker : "div"} entries=input.entries/>
<${input.view === "ticker" ? Ticker : "div"} entries=input.entriesB/>
