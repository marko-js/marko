// size: 148 (min) 86 (brotli)
(_._script(`a0`, ($scope) => {
  if ($scope.e !== `a`) throw Error(`did not serialize a`);
}),
  _._script(`a1`, ($scope) => {
    if ($scope.e !== `a`) throw Error(`did not serialize input.a`);
  }),
  init());
