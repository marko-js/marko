<let/show=true/>
<main>
  <if=show>
    <counter/{ value }/>
    <p>${value}</p>
    <button.reset onClick() { value = 0 }>r</button>
  </if>
  <button.toggle onClick() { show = !show }>t</button>
</main>
