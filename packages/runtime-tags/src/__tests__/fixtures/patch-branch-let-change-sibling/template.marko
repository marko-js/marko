<store/store/>
<main>
  <h1>${input.title}</h1>
  <p>Last ${store.last}</p>
  <if=input.show>
    <let/count=0 valueChange=store.set/>
    <span>Seen ${count}</span>
    <button onClick() { count++ }>+</button>
  </if>
</main>
