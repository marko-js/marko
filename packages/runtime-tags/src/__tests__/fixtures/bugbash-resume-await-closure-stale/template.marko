import { resolveAfter } from "../../utils/resolve";
<let/count=0/>
<button onClick() { count++ }>inc</button>
<div>count:${count}</div>
<try>
  <await|value|=resolveAfter("v", 1)>
    <span>${value} closure:${count}</span>
  </await>
  <@placeholder>loading</@placeholder>
</try>
