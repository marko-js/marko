// template.marko
var template_default = _template("a", (input) => {
	_scope_reason();
	const $scope0_id = _scope_id();
	_html(`<div>${_text_resume($scope0_id, "a", void 0)}</div>`);
	_script($scope0_id, "a0");
	_scope($scope0_id, {});
}, 1);
