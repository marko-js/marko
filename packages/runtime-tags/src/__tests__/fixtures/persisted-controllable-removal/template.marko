<const/handler=(next: string) => { document.querySelector("main")!.dataset.got = next }/>
<main>
  <h1>${input.title}</h1>
  <input value=input.value valueChange=(input.wire ? handler : undefined)>
</main>
