// total: 1464 (min) 804 (brotli)
// template.marko: 85 (min) 63 (brotli)
const $promise = /* @__PURE__ */ _const(0, _script("a0", ($scope) => (async () => {
	document.getElementById("ref").textContent = await $scope.a;
})()));
