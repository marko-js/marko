import { resolveAfter } from "../../utils/resolve";
static function run(fn: () => void) {
  return typeof fn;
}
<let/on=1>
<const/unused=run(() => { on++ })>
<try>
  <@placeholder>loading...</@placeholder>
  <await=resolveAfter(0, 4)>
    <child q=1 on=on/>
  </await>
</try>
