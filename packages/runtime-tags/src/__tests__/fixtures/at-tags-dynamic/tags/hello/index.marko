for|{ content, ...attrs }| of=input.list.item
  div.item ...attrs
    ${content}

for|{ content, row, ...attrs }| of=input.col
  div.col ...attrs
  for|{ content, ...attrs }| of=row
    div.row ...attrs
      ${content}
