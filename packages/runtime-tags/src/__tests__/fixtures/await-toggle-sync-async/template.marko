import { resolveAfter } from "../../utils/resolve";
<let/showAsync = false/>
<await|value|=showAsync ? resolveAfter("ASYNC") : "SYNC">
  Got: ${value}
</await>
<button onClick() { showAsync = !showAsync }>toggle</button>
