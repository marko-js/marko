// A lazy child rendered inside constructed content: server-fed input
// plus its own client state, so a compiled persisted merge registers on load.
<let/taps=0/>
<div class="gadget">
  <span class="gadget__label">${input.label}</span>
  <button class="gadget__tap" onClick() { taps++ }>taps ${taps}</button>
</div>
