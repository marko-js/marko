import Child from "./child.marko" with { load: "render" }

<let/clicks=0/>
<button class="page" onClick() { clicks++ }>${clicks}</button>
<try>
  <@placeholder>loading</@placeholder>
  <Child/>
</try>
