# Render
```html
<div>
  <b>
    open
    <span>
      after
    </span>
  </b>
</div>
<b>
  <button>
    set
  </button>
</b>
```
## Console
```
ERROR "An unescaped placeholder (`$!{}`) rendered HTML that does not parse as a balanced fragment where it is placed (an unclosed, stray or misnested tag), so updating it removes the wrong nodes."
```
