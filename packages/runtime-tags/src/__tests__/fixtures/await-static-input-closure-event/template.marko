import { resolveAfter } from "../../utils/resolve";

<let/count=0>

<try>
  <@placeholder>Loading</>
  <await|d|=resolveAfter({ items: ["a", "b"] }, 1)>
    <p>${input.foo}</p>
    <button onClick() { count++; }>${count}</button>
  </await>
</try>
