<let/note=""/>
<fieldset>
  <input.a type="radio" checkedValue=input.picked checkedValueChange(next: string) { note = next } value="a">
  <input.b type="radio" checkedValue=input.picked checkedValueChange(next: string) { note = next } value="b">
</fieldset>
<p>${note}</p>
