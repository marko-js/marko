# Diagnostics

- `template.marko` warning: A `>` in this tag's value closed the tag, so the comparison is truncated and the rest is rendered as text. Wrap the value in parentheses.
- `template.marko` warning: A `>` in this tag's value closed the tag, so the comparison is truncated and the rest is rendered as text. Wrap the value in parentheses.
