<let/count=0/>
<button.count onClick() { count++ }>clicked ${count}</button>
<ul>
  <for|item| of=input.items by(item) { return item.id }>
    <let/n=0/>
    <li class="item">${item.name}<button.tap onClick() { n++ }>tap ${n}</button></li>
  </for>
</ul>
