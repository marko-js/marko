<main>
  <try>
    <div>
      <if=input.show>
        <await|a|=input.fast><b>${a}</b></await>
      </if>
      <await|b|=input.slow><em>${b}</em></await>
    </div>
    <@placeholder><i>loading</i></@placeholder>
  </try>
  <button onClick() {}>x</button>
</main>
