import Child from "./child.marko"

<const/item={ label: input.label }>
<main>
  <Child name="a" item=item/>
  <Child name="b" item=item/>
  <try>
    <@placeholder>loading</@placeholder>
    <await|value|=input.promise>
      <span>${value}</span>
      <Child name="c" item=item/>
    </await>
  </try>
</main>
