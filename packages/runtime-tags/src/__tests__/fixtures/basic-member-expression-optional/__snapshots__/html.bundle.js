// template.marko
var template_default = _template("a", (input) => {
	_scope_reason();
	const $scope0_id = _scope_id();
	let index = -1;
	_html(`<div>${_text_resume($scope0_id, "a", void 0)}</div><div>${_text_resume($scope0_id, "b", void 0)}</div><button>Update</button>${_el_resume($scope0_id, "c")}`);
	_script($scope0_id, "a0");
	_scope($scope0_id, { d: index });
}, 1);
