import Plain from "./tags/plain.marko";

// The component renders the attribute tag's content where it is and never
// sends it, so nothing that content reads reaches the client.
<heading type=Plain text="Hello"/>
