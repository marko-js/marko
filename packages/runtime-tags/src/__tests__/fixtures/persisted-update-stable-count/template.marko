import { CHIPS } from "./data";

<let/count=0/>
<button.count onClick() { count++ }>clicked ${count}</button>
<nav>
  <for|chip| of=CHIPS>
    <a class=["chip", { active: $global.pick === chip }]>${chip}</a>
  </for>
</nav>
