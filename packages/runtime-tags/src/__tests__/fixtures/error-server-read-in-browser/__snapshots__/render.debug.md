# Render
```html
<button>
  x
</button>
```

# Update
```js
container.querySelector("button").click();
```
## Error
```
"boss" was declared in a `server` statement and is unavailable in the browser. Use a `<const>` tag instead if the value is needed by the template.
```
