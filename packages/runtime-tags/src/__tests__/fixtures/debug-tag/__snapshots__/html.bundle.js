// template.marko
var template_default = _template("a", (input) => {
	_scope_reason();
	const $scope0_id = _scope_id();
	debugger;
	debugger;
	let y = 0;
	debugger;
	_script($scope0_id, "a0");
	writeScope($scope0_id, { b: y });
	_resume_branch($scope0_id);
}, 1);
