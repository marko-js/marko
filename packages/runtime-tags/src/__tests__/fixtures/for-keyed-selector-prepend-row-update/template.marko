<let/items=[1, 2]/>
<let/selected=0/>
<button.prepend onClick() { items = [0, ...items] }/>
<for|item| of=items by=(x) => x>
  <let/m=0/>
  <const/active=selected === item/>
  <button class="row" + item onClick() { m++; selected = item }>${m + ":" + active}</button>
</for>
