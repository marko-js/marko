<main>
  <h1>${input.title}</h1>
  <if=input.a>
    <let/x=0/>
    <p.pa>A ${x}</p>
    <button.ba onClick() { x++ }>+</button>
  </if>
  <if=input.b>
    <let/y=10/>
    <p.pb>B ${y}</p>
    <button.bb onClick() { y++ }>+</button>
  </if>
</main>
