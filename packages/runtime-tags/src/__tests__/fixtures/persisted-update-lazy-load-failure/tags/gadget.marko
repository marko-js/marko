import { assertChunkLoadable } from "../chunk-guard";
static assertChunkLoadable();

<let/taps=0/>
<div class="gadget">
  <span class="gadget__label">${input.label}</span>
  <button class="gadget__tap" onClick() { taps++ }>taps ${taps}</button>
</div>
