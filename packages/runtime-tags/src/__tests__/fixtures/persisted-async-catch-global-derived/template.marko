<const/tag=`${$global.brand}!`/>
<let/n=0/>
<main>
  <try>
    <await|value|=input.promise>
      <em>${value}</em>
    </await>
    <@catch><p>${tag}</p></@catch>
  </try>
  <button onClick() { n++ }>${n}</button>
</main>
