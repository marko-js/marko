<let/count=0/>
<button onClick() { count++ }>${count}</button>
<try>
  <await=input.promise>
    <div id="done">${input.msg} done</div>
  </await>
  <@placeholder>
    <em>loading</em>
  </@placeholder>
</try>
