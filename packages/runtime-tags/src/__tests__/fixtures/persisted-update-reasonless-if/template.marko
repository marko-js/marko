import { showPrimary } from "./data";

<let/count=0/>
<button onClick() { count++ }>count ${count}</button>
<if=showPrimary()>
  <p.primary>primary</p>
</if>
<else>
  <p.fallback>fallback</p>
</else>
