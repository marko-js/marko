<let/list=["a", "b"]/>
<for|item, ...meta| of=list>
  <div>${item}:${meta[0]}:${meta.length}</div>
</for>
<child|first, ...others|>
  <div>${first}|${others[0]}|${others[1]}|${others.length}</div>
</child>
