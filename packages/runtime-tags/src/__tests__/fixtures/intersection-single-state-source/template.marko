<let/count=1/>
<const/doubled=count * 2/>
<const/tripled=count * 3/>

<button onClick() { count++ }>increment</button>
<div>
  -- ${count}
  -- ${doubled}
  -- ${tripled}
  -- ${doubled + tripled}
</div>
