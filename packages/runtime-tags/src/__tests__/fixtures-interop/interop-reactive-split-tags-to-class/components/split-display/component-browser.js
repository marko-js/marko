// Split component (browser class makes it split / attach-only as a hydration
// root). The Tags API parent reactively updates `value`, which must re-render
// this child in the browser.
module.exports = {};
