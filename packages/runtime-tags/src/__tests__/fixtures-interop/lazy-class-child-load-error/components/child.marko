static {
  // Simulates a failed lazy chunk load: rejects the browser side dynamic
  // import while leaving the server render untouched.
  if (typeof window !== "undefined") {
    throw new Error("load failed");
  }
}

<span id="child">${input.value}</span>
