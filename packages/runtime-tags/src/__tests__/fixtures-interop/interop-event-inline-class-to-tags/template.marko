class {
  onCreate() {
    this.state = { pinged: "none" };
  }
  handlePing(count) {
    this.state.pinged = count;
  }
}
<div id="class">${state.pinged}</div>
<tags-pinger onPing(count) {
  component.handlePing(count);
}/>
<init-components/>
