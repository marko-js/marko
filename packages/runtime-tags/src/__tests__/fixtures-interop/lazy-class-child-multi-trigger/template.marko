import Child from "<child>" with { load: "visible body|idle?timeout=100" }

class {
  onCreate() {
    this.state = { value: 0 };
  }
  increment() {
    this.state.value++;
  }
}

<button id="inc" onClick("increment")>Inc</button>
<Child value=state.value/>
<init-components/>
