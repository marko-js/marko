import Child from "<child>" with { load: "media(width <= 600px)" }

class {
  onCreate() {
    this.state = { value: 0 };
  }
  increment() {
    this.state.value++;
  }
}

<button id="inc" onClick("increment")>Inc</button>
<Child value=state.value/>
<init-components/>
