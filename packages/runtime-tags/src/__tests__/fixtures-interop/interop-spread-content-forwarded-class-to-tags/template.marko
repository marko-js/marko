class {
  onCreate() { this.state = { n: 0 }; }
  inc() { this.state.n++; }
}
<button id="class" onClick("inc")>${state.n}</button>
<tags-forward id="forwarded">Hello ${state.n}</tags-forward>
<init-components/>
