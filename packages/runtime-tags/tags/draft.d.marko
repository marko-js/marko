/** File for types only, not actual implementation **/

export interface Input<T> {
  value: T;
}

// A guess assigns the variable; the handler only exists for the type-checker.
return=(input.value as T) valueChange=((_guess: T) => {})
