/** File for types only, not actual implementation **/

export interface Input<F extends (...args: any[]) => any = (value: any) => any> {
  value?: F;
}

return=(input.value as F & { readonly pending: boolean })
