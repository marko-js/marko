<const/{name, write}=input/>
<div>${name} a</div>
<span>${name} a</span>
<p>${name} a</p>
<script>
  write(`${name} mounted`);
  $signal.onabort = () => {
    write(`${name} destroyed`)
  };
</script>