<let/show=true/>
<button onClick() { show = !show }>Toggle</button>

<pre/el/>

<if=show>
  <div>child</div>
  <script>
    el().innerHTML += '\nmounted';
    $signal.onabort = () => {
      el().innerHTML += '\ndestroyed';
    };
  </script>
</if>