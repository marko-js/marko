# Task: Notes never appear

Bug report from a user of the recipe app:

> On a recipe page, I type a cooking note and click "Add note". The input clears, but the note never shows up in the notes list — even though I definitely clicked the button. No errors in the console.

## Requirements
- Find and fix the bug. After the fix, adding a note shows it in the notes list immediately (no reload), and adding a second note shows both, in order.
- Keep the fix minimal; do not rewrite unrelated parts of the app.

## Output
Return only the files you create or modify (complete contents), paths relative to the app root.
