<button attrs="data.rootAttrs"
    w-onclick="handleClick"
    w-bind="./widget">

    <if test="data.label">
        <span w-el-id="label">${data.label}</span>
    </if>
    <invoke function="data.renderBody(out, widget)"
        else-if="data.renderBody"/>
</button>