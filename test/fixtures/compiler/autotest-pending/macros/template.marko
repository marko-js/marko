<macro renderTree(node)>
    Name: ${child.name}

    Children:
    <ul if(node.children)>
        <li for(child in node.children)>
            <renderTree(child)/>
        </li>
    </ul>
</macro>

<renderTree(data.node)>