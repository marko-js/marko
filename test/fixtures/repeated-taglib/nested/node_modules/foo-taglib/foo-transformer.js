exports.process = function (node, compiler, template) {
    if (node.data.fooTransformer === true) {
        throw new Error('foo-transformer.js called twice on the same node!');
    }

    node.data.fooTransformer = true;
};