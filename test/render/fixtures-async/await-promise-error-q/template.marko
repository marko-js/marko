<await(promiseData from data.promiseData)>
    BEFORE
    <if(true)>
        ${promiseData.noprop.value}
    </if>
    AFTER
    <await-error>
        something went wrong!
    </await-error>
</await>
