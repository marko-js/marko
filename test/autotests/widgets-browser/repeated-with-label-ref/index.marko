class {

}

<div>
    <for(index, field in input.fields)>
        <div>
            <input ref="field[${index}]" type="text" value=field.value/>
            <label for-ref="field[${index}]">${field.label}</label>
        </div>
    </for>
</div>