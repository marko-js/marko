<div w-bind>
    Hello ${data.name}!
</div>

<script>
    module.exports = {
        onInput: function(input) {
            this.state = {
                name: input.name
            };
        },
        setName: function(newName) {
            this.setState('name', newName);
        }
    };
</script>