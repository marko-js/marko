class {}

<macro renderButton(color)>
    <button on-click('handleColorClick', color)>
        ${color}
    </button>
</macro>

<div>
    <for(color in ['red', 'green', 'blue'])>
        <renderButton(color)/>
    </for>
</div>