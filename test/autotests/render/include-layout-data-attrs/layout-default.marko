<h1 if(input.showHeader !== false)>
    <if(input.header)>
        <include(input.header)/>
    </if>
    <else>
        DEFAULT TITLE
    </else>
</h1>
<div>
    <include(input.body)/>
</div>
<h1 if(input.showFooter !== false)>
    <if(input.footer)>
        <include(input.footer)/>
    </if>
    <else>
        DEFAULT FOOTER
    </else>
</h1>