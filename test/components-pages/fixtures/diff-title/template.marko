class {
    onCreate () {
        this.state = {
            count: 0
        }
    }
    onMount () {
        window.app = this;
    }
    increment () {
        this.state.count++;
    }
}

<!DOCTYPE html>
html lang="en"
    head
        meta charset="UTF-8"
        title -- Count: ${state.count}
    body

        div id="test"
        div id="mocha"
        div id="testsTarget"

        init-components immediate
