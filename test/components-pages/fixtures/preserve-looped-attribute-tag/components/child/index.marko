class {}

<ul>
    <for|item| of=input.items>
        <li>
            <if(typeof item === "string")>${item}</if>
            <else>
                <${item}/>
            </else>
        </li>
    </for>
</ul>