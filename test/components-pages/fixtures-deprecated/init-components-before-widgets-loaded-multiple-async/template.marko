<!DOCTYPE html>
html lang="en"
    head
        meta charset="UTF-8"
        title -- Marko Components Tests
    body

        div id="test"
        div id="mocha"
        div id="testsTarget"

        <await(foo from Promise.resolve())>
            <app-foo/>
        </await>
        <await(foo from Promise.resolve())>
            <app-foo/>
        </await>
