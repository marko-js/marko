class {
    onCreate() {
        window.helloInstances.push(this);
    }

    onInput(input) {
        this.state = {
            name: input.name
        }
    }
}

<div.hello key="root">Hello ${state.name}</div>
