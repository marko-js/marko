class {
    onCreate() {
        this.state = {
            count: 1
        };
    }

    increment() {
        this.state.count++;
    }
}

<for|i| from=0 to=(state.count - 1)>
    <div>${i}</div>
</for>