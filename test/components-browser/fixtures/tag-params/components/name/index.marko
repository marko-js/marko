class {
    onCreate() {
        this.state = {
            name:'Anna'
        }
    }
    changeName() {
        this.state.name = 'Vickie';
    }
}

<div>
    <include(input.renderBody, state.name)/>
    <button on-click('changeName')>
        Change
    </button>
</div>