import Card from "./Card.marko"
<Card title="Welcome">
  <p>This is a big chunk of static body content passed into the card.</p>
  <p>It never changes on the client and the card never re-renders it.</p>
</Card>
<let/n=0/>
<button onClick() { n++ }>${n}</button>
