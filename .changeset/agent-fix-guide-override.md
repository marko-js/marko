---
"@marko/compiler": patch
---

Honor a `MARKO_AGENT_FIX_GUIDE` environment variable that force-enables (`1`) or suppresses (`0`) the coding-agent fix-guide pointer regardless of detected agent markers.
