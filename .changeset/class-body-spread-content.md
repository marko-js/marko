---
"marko": patch
"@marko/runtime-tags": patch
---

Render a Class API parent's body, and the bodies of its attribute tags, when a Tags component spreads them onto a native tag (`<div ...input/>`) rather than rendering them with `<${input.content}/>`.
