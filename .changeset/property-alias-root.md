---
"@marko/runtime-tags": patch
---

A property read through a renamed value (`<const/a=item/>` then `a.id`, or `rest.label` after `<const/{ ...rest }=input/>`) is the root value's property: one read, and a child template asks its caller for that property alone. Dev runtime entries list every dom feature module so the optimizer sees them up front.
