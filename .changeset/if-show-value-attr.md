---
"@marko/runtime-tags": patch
---

`<if value=cond>`, `<else-if value=cond>` and `<show value=cond>` now compile like the `=` shorthand, matching every other core tag's handling of the written-out attribute.
