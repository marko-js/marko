---
"@marko/runtime-tags": patch
"marko": patch
"@marko/compiler": patch
---

Add the native `<search>` and `<slot>` HTML elements to the HTML taglib and `tags-html.d.ts` type definitions so they get proper attribute completions and type checking.
