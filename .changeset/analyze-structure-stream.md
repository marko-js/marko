---
"@marko/runtime-tags": patch
---

Refactor the translator to record the client template and walk data during analysis as a per-section stream that each output resolves. Compiled output is unchanged.
