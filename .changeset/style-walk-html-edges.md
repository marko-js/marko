---
"@marko/runtime-tags": patch
---

Fix the client walk for a `<style>` with dynamic rules followed by sibling elements, and move a branch's edges with an unescaped `$!{}` hole that is the branch's whole content.
