---
"@marko/runtime-tags": patch
"marko": patch
---

A Tags API dynamic tag renders a Class API component or body even when its template loaded before the Class API compat runtime. Interop bundles without a Tags dynamic tag no longer ship the dynamic tag runtime.
