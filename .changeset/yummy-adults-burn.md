---
"@marko/runtime-tags": patch
---

Fix bound attribute refinement modifier on a destructured value forwarding the modified value through the change handler instead of reassigning the destructured `const` binding.
