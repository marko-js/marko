---
"@marko/runtime-tags": patch
"@marko/compiler": patch
---

Clarify the dynamic `<style>` placement warning, document the `Marko.Global` augmentation path for typing `$global`, cover `static` and the `<let>`-as-a-formula pitfall in the cheat sheet, and mark the four compiler options that only the Marko 5 translator implements.
