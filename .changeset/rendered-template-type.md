---
"@marko/runtime-tags": patch
"marko": patch
---

Add `pipe`, `toString`, `catch`, and `finally` to the internal `RenderedTemplate` type, matching the published `Marko.RenderedTemplate` and the server implementation.
