---
"@marko/compiler": patch
---

Discover custom elements declared only as `customElement` class declarations with a `tagName` (eg Shoelace's manifest), and resolve manifest module paths relative to the manifest's directory when they are not package-root relative.
