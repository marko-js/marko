---
"@marko/runtime-tags": patch
---

Point an `<effect>` with body content at its replacement, `<script>`, which runs its body as code, instead of reporting that `<effect>` does not support body content.
