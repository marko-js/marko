---
"@marko/runtime-tags": patch
---

Register an attribute tag's content that a child passes to `<await>` as its value and first renders on the client, which failed the server render with `Unable to serialize`.
