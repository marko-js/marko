---
"@marko/runtime-tags": patch
---

Name the actual handler attribute (`checkedChange`, `checkedValueChange`, `openChange`, …) in the server-side controllable debug assertion instead of always reporting `valueChange`.
