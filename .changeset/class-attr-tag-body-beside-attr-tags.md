---
"marko": patch
---

Pass a Class attribute tag's body to a Tags parent as `content` when that attribute tag also has nested attribute tags, instead of as a `renderBody` the Tags component could not render.
