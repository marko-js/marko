---
"@marko/runtime-tags": patch
---

Warn in development when a server-rendered lazy `load` trigger cannot find its selector and the module is fetched immediately, matching the warning the client already emits.
