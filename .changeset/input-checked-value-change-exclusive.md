---
"@marko/runtime-tags": patch
---

Report `checked` combined with `valueChange` on an `<input>` as mutually exclusive, instead of a serialization error on the server and a handler-type error on the client.
